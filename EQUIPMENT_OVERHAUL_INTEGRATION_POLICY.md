# 装備オーバーホール各論（防具・盾・近接メタ）

> **このファイルの役割** — 防具・盾・近接メタ・スリングの詳細方針を定義する。  
> 弓／クロスの設計詳細は **[`BOW_DESIGN_TARGETS.md`](BOW_DESIGN_TARGETS.md)**、実装・現状・チェックリストは **[`BOW_IMPLEMENTATION_STATUS.md`](BOW_IMPLEMENTATION_STATUS.md)**。入口は **[`BOW_MOD_INTEGRATION_POLICY.md`](BOW_MOD_INTEGRATION_POLICY.md)**。

## 防具・盾の再定義

近接職の継戦性を上げ、退却を含む実戦的な防衛行動を成立させるための方針。

### 重装備・盾の共通方針

- **負傷の肩代わりを重視**: 防御は「被ダメ軽減」だけでなく「負傷デバフの発生抑制」を主目的に置く。
- **退却可能性を守る**: 後退時の事故死を減らすため、盾運用は背面被弾リスクも含めて設計する。
- **運ゲーを減らす**: 防具は可能な限り「安定して守る」設計を優先し、偶発的な抜けを抑える。

### 防具カテゴリ方針

- **ギャンベゾン（機動防具）**: 移動力をほぼ殺さず、遠隔職の事故被弾を抑える標準防具。
- **重装鎧（鎖帷子・板金）**: 機動低下と引き換えに、被弾の安定受けと継戦性を担う前衛装備。
- **カイトシールド**: 進軍・後退の両局面で脚部負傷リスクを抑える「前衛の継戦装置」として扱う。

## 近接メタ武器（斧）の役割

- **対盾の突破口**: 斧・投斧は盾耐久への圧力役とし、射手ラインへの通し道を作る担当に寄せる。
- **対装備メタの循環を維持**: 盾を強化した場合、斧側にも対抗手段を残して装備相性を循環させる。

## スリングの役割（弓・クロス外レーン）

スリングは弓／クロスとは別系統として扱い、最長射程主役や重装キラーの主軸にしない。

- **片手スリング（`sling` / `OneHandSling`）**: 盾併用を前提に、近中距離の撹乱・妨害を担当。
- **スタッフスリング（`sling_staff` / `TwoHandSling`）**: 片手より射程と圧力を持たせるが、主砲化は避ける。
- **Stagger 付与の検討**: 盾持ち前衛の足を止める補助武器としての価値を重点評価する。

### Mod 数値（スリング微調整・2026-04）

**正本**: `scripts/apply_ranged_equipment_delta.py` の `SLING_COMBAT`（`apply_sling_overlays` が `primaryWeaponMode` の該当キーのみマージ）と **`Data/Models/Equipment.json`** の `sling` / `sling_staff`。弓 7 種の再生成のあと **同スクリプト**でスリングも上書きされる。

- **`sling`**: `ignoresArmor` をやや抑え、**弓・クロス的な「装甲越え主役」読みに寄せない**（鈍弾レーン）。`attackSpeed` をわずかに上げ、**近中の撹乱**寄りの手触りにする。
- **`sling_staff`**: 現状の強さに問題が見えないため、**攻速などの弱体は入れない**（片手との射程・単発差は従来どおりバニラ土台＋必要なら別途のみ）。
- **UI**: 弓と同様、**装甲貫通の段表記は `ignoresArmor` と必ずしも一致しない**前提。数値判断は JSON と実戦。

## 全装備の整合チェック

1. **単独カテゴリ強化をしない**: 武器・防具・盾のいずれかだけが一方的に得をしないか確認する。
2. **同ティア比較を必須化**: 射撃 vs 近接、軽装 vs 重装、盾あり vs 盾なしをセットで比較する。
3. **勝ち筋の複線化**: 遠隔引き撃ちだけ、重装押し付けだけ、の一択を避ける。
4. **チャート + 実戦で判断**: 期待値グラフだけで確定せず、編成別のプレイ確認を実施する。

## 実装時の注意（データ編集）

- 変更は `Data/Models/Equipment.json` の部分上書きで実施（構造は `Items/Equipment.json` 準拠）。
- 防具・盾・近接武器を触る際も、基準はバニラ `Items/Equipment.json`。
- 参照は「バニラ + 本 Mod 差分」に限定し、他 Mod 定義は判断基準に使わない。

## 弓・クロスの反映状況（2026-04-13）

1. **弓の移動ペナルティ（設計目標）**
   - 明示倍率で整理する目標（`short=1.00`, `curved=0.95`, `war=0.85`, `long=0.75`）。**現行の `Equipment.json` では `onEquipEffectors` による移動特徴付けは行わない**（2026-04-16）。トレードオフは `range` / `attackSpeed` / 掩体などで表現する。
2. **クロスボウの移動制約（設計目標）**
   - 明示倍率で整理する目標（`light_crossbow=0.85`, `crossbow=0.75`, `heavy_crossbow=0.70`）。**データ上の装備時移動は付けない**（同上）。
   - **意図**: ライトは `war_bow` 相当、標準〜ヘビィは `long_bow`（0.75）以上の鈍重帯に置く、という**読み方の参照**として残す。
   - **補足（バニラ effector を使わない理由のメモ）**: `ImpairedMovementLow` / `Med` は移動以外に **`RangedSpeed`（射撃間隔）・`EvadeChance`** が乗り、素体で既に遅い `attackSpeed` と二重になりやすい。かつ Mod 独自の `Effectors.json` は現行ビルドでマージされにくい。**移動のみの段階付け**が必要になったら、本体側の Mod Effector 対応や別手段を検討する。
3. **クロスボウの `rangedCover`**
   - **採用済み方針**: **バックラー（最も遠距離カバーが低い盾）を上限基準**とし、クロスの `rangedCover` は常にバックラー未満に置く。
