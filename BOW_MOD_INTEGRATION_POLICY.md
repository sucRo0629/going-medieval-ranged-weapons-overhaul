# 弓・クロス データ変更方針（`BOW_MOD_INTEGRATION_POLICY.md`）

> **このファイルの役割** — 弓／クロス調整の**バランス前提**（役割の長所・短所、被弾メタ、カタログ DPS の見方、Q3 鎖、装備中の移動・掩体、命中・装甲無視の整理、近接との比較、高 Q ガード、Marksman 門限、変更時チェック）。Mod の目的・スコープ・バニラの開き方・スリング・素体×品質の一般論は **[`CREATION_POLICY.md`](CREATION_POLICY.md)**。

[`CREATION_POLICY.md`](CREATION_POLICY.md) の **スコープ** と **順序の約束**に従う。**素体・門限の実装の正**は **本 Mod の** `Data/Models/Equipment.json`（`requiredSkills` 含む）。品質は **`WeaponQualitySettings.json`** の乗算がさらに掛かる。本文・表と実装の突き合わせは **バニラ `Items/Equipment.json`** と **本 Mod の `Data/Models/Equipment.json`** のみとし、**他 Mod の `Equipment.json` は参照しない**。

---

## 記号

- **`a < b`** … 同一フィールドの数値として **左が右より小さい**（狭義の不等号）。`attackSpeed` は **数値が小さいほど速い**ため、「遅い＝間隔が長い」ときは数値が大きくなる。

---

## 弓とクロスボウの差別化（役割）

### **弓（TwoHandBow）**

長所

- **長射程**： 使用する矢と射出角度によりクロスボウよりも遠くまで届く。（基本的に同格以上のクロスボウより `range` が大きい）
- **高回転**： 巻き上げを必要とするクロスボウと比較して連射できる。`attackSpeed`（数値が小さいほど速い）で表現。
- 品質が高いほど性能がよい

短所

- 遠いほど外れやすい：**`precisionFalloff` はクロスより大きめ**に調整。
- 低威力： 単発 `damage` は押し上げず（長弓はバニラ上限を超えない想定）、**射程・命中曲線・品質**で実効を取る。
- 品質が悪いほど性能が低い（品質による影響が大きい）

### **クロスボウ（TwoHandCrossbow）**

長所

- **中射程の階段**：ライト〜ヘビーで届く距離に段があり、**長弓より短い**帯に置く（弓の「最長レンジ主役」と棲み分け）。
- **据え撃ち・一発向き**：`attackSpeed` は遅め（数値が大きい）。**素体 `attackSpeed` はバニラのライト／標準／ヘビーと同値**に寄せ、据え撃ちの手触りをバニラに揃える。
- **命中の減衰が緩い**：**`precisionFalloff` は弓より小さめ**で、距離が伸びても命中率が落ちにくい。
- **鎧・単発火力**：`damage` / `ignoresArmor` で鎧向きの顔を出す。**短射程・高精度・`ignoresArmor` の代償**として単発 `damage` はバニラ素体より上げてよいが、**過剰インフレは避け**、チャートとプレイで確認する。
- 冒頭の大方針どおり、**品質・素材でダメージを伸ばして**実効を積み上げられる。

短所

- **連射は弓に劣る**：引き金式で、弓のような試行回数は出しにくい。
- **射程は弓最上級より届かない**：中射程帯に縛られ、遠くを取る役は弓側に寄せる前提。

### 共通箇所

- **本体の Marksman**: 命中・威力などへの補正は弓とクロスに**同様に掛かる**前提。違いは **`Equipment` の静特性**（＋任意で `WeaponQualitySettings`・`requiredSkills`）で出す。装備門限の方針は**下記「装備門限」節**。
- **調整の見方**: カタログ上の「期待 DPS」だけで裁かない。**届く距離・減衰・門限・品質**を並べ、プレイで確認する。対応ランク同士では弓の素体期待をクロスよりやや低めに寄せる、などの**相対**がありうる（具体数値は都度 `Equipment` と検証で決める）。
- **順序の約束**: 上級ラインでは `range` / `damage` / `ignoresArmor` / `attackSpeed` の **`<` 連鎖**（クロスと上級弓の段差）を崩さないこと。詳細の鎖は**下記「Q3 素体での相対チェーン」**。
- **装備中の移動（本職の長射程のコスト）**: バニラ鎧と同系の **`onEquipEffectors`** で **移動速度低下**を段階付けする。**射程・火力を削らず**に立ち回りコストを置き、**片手スリング・投擲（`OneHandThrow`）**との差を付ける。鎧の `ImpairedMovement` と**重ね掛け**されうるのでプレイで確認する。
  - **低下なし**: **`short_bow`**（軽量弓）、**`light_crossbow`**（ライト）。
  - **弱い（`ImpairedMovementLow`）**: **`war_bow`**、**`curved_bow`**、**`long_bow`**、**`crossbow`**（標準）。
  - **中程度（`ImpairedMovementMed`）**: **`heavy_crossbow` のみ**。**最重の代償に火力素体をライト／標準より余計には盛らない**（`damage` / `range` / `ignoresArmor` は段差の連鎖内に留める）。**`ImpairedMovementMed` の報いは掩体性能**（主に **`meleeCover` / `coverAngle`**。**`rangedCover`** はバニラが 0 が多いため、効果が確認できてから足す）で出し、据え撃ち時の生存を厚くする。具体は常に `Equipment` が正。
  - **掩体（`meleeCover` / `coverAngle` / `rangedCover`）**: クロス段差で **ヘビィはライト・標準より掩体を厚く**（**Med 移動**とセット）。**ライト**は機動枠として掩体は中程度（バニラ比でやや上げてもよい）、**標準**はバニラ相当でよい。**`rangedCover`** を 0 以外にする場合は本体挙動を確認する。

---

## 被弾減速メタと「届く・当たる」、カタログ DPS

- **ゲーム性**: 攻撃が当たると**移動速度が非常に遅くなる**ため、**射程の実戦価値が大きい**。加えて**怪我による能力デバフ**が乗るため、**被弾しやすい近接**は負担が重い（下記「同ティアの近接武器との比較」）。現状のクロスボウ系は、**カタログ上の DPS が低くても**その射程帯では**一択級に強く感じられうる**（プレイ体感・メタの前提）。
- **弓を長射程にする場合（2026-04-12 ユーザー方針）**: **射程を伸ばした弓系は、計画表と同じ式によるカタログ期待 DPS をクロスボウ系より低く抑える**のが適正（**届く距離・`precisionFalloff`・品質・Marksman 門限**で実効を取りにいく）。**序盤**の `short_bow` と `light_crossbow` の比較は**低スキル目標**（計画書「研究・スキル」節）を優先し、上級帯ほどこの「弓の表見 DPS はクロスより下げる」原則を強く適用する。
- **命中**: **距離による命中減衰**（`precisionFalloff` 等）と Marksman 補正で、**どの距離でどの程度当たるか**が実効を決める。表の期待 DPS 式はあくまでプロキシ。
- **距離の目安（出発点・具体数値は後定）**: **クロスボウ系が概ね安定して当てられる距離**より**やや遠い位置**では、弓は**ほぼ当たる**程度を**初期の調整目標**としてよい（**無反動スナイパ**にならないよう、`precisionFalloff` と最大射程の組み合わせは必ずプレイで確認する）。

---

## Q3 素体での相対チェーン（`crossbow` / `heavy_crossbow` / `curved_bow` / `long_bow`）

**「長所の分担」**を、**製作品質 Q3** の合成（素体＋`WeaponQualitySettings` の通常行）で次の **狭義 `<`** として維持する目標とする。記号は**同一 JSON フィールド**の比較。

- **`range`（届く距離が短い → 長い）**: `crossbow` < `heavy_crossbow` < `curved_bow` < `long_bow`
- **`damage`（単発が低い → 高い）**: `long_bow` < `curved_bow` < `crossbow` < `heavy_crossbow`
- **`ignoresArmor`（低い → 高い）**: `long_bow` < `curved_bow` < `crossbow` < `heavy_crossbow`（**厳密 `<`**）。`curved_bow` の `ignoresArmor` を上げすぎると **`crossbow` を上回り連鎖が崩れる**ため、Q3 合成では**この順序を満たす値に揃えて**調整する。
- **`attackSpeed`（間隔が短い＝速い → 長い＝遅い）**: `curved_bow` < `long_bow` < `crossbow` < `heavy_crossbow`

**補足**: **`light_crossbow`** は序盤枠のため、`range` は **`crossbow` 未満（または同帯の最短）**とし上記と矛盾させない。**`short_bow` / `war_bow`** は別ライン。弓階段では **`short_bow` < `war_bow` < `curved_bow` < `long_bow`** の `range` 階段と整合させる。

---

## 命中・装甲無視（調整の整理）

- **弓**: 段階は短 ＜ 戦 ＜ 曲 ＜ 長。曲弓の素体 `precision` は **最大 0.99**、長弓は品質乗算後に **Q3 以降で実効 1** になるよう素体を置く、といった整理でよい（具体数値は常に `Equipment` が正）。
- **クロスボウ**: 素体 `precision` はライト → 標準 → ヘビーで段を付け、ヘビーは高品質で頭打ち寄りにできる。**`ignoresArmor` はクロス系が弓より高い**前提で調整する。
- **曲弓 `ignoresArmor`**: バニラ `curved_bow` と同じ **0.8** に合わせる（バニラを正）。

---

## 同ティアの近接武器との比較（検証に含める）

- **射撃同士だけで完結させない**: 弓・クロスを調整したあと、**同じ研究・資源あたりのティアに位置する近接武器**（バニラ `Equipment` の対応 `id`。具体は詳細計画で抜粋）と**並べて強すぎないか**を確認する。カタログ DPS や届きだけでなく、**役割の被り**（近接が担うはずのリスク仕事を射撃だけで代替できないか）を見る。**近接を一貫して上回る**調整はしない。
- **被弾側の負担（ゲーム性）**: 攻撃が命中すると**移動速度が大きく落ちる**（上記「被弾減速メタ」）に加え、**怪我により各種能力にデバフ**が付く。**射撃を受ける前に殴られやすい近接**は、これらのペナルティを**より高頻度で被りやすい**前提に立つ。
- **含意**: 上記を踏まえ、射撃を**さらに単発・期待 DPS で押し上げない**。検証は**前衛が前に出る編成・同ティア近接との比較**をセットにする（プレイ・メモ。数値閾値は後定）。

---

## 高品質弓が「常に最強」にならないためのガード

品質で弓が伸びると、**終盤だけ弓一択**になる懸念がある。次を**方針として同時に満たす**（具体数値は詳細計画・プレイで調整）。

- **役割の帯域**: 弓の優位は **遠距離・高 Q・上級 id** に寄せ、**中射程での即応・鎧向き・据え撃ち**ではクロスが地板として残るよう、`range` / `precisionFalloff` / 単発帯を**用途で分ける**（全距離・全品質で弓が上回らない）。
- **相対曲線**: **同条件の一部帯（例：Q3 付近・中距離）ではクロスが上、高 Q や高スキル弓は遠距離で逆転**のような**交差する強弱**を検証目標にする。詳細計画の **長弓 vs ヘビークロス品質別**の考え方（低 Q はクロス寄り、高 Q で弓が伸びる）と整合させる。
- **WQS の上端**: `TwoHandBow` の**高品質乗算に天井**を設け、**無制限にクロスを越え続けない**ようにする選択肢を許容する（ドラフト表の見直し）。
- **経済と地板**: **弓安価・クロス割高のレシピ関係は維持**するが、**コストが高いからクロスを弱くする**整理はしない（**コストと戦闘性能の切り離し**）。「弓が常勝しない」理由も **コスト差に依存しない**（終盤はコストが効きにくい）。**帯域・相対曲線・WQS 上端・`damage` 枠**で弓とクロスの併存を作る。
- **単発の枠**: **`damage` はバニラ水準を超えない**。品質で伸びるのは主に**射程・命中・間接的な実効**に寄せ、**数値インフレでクロスを恒久的に下位にしない**。

---

## 装備門限（`requiredSkills` / Marksman）

**本 Mod の `Data/Models/Equipment.json` にある `requiredSkills` が確定値**であり、下表の数値は**ドラフト（計画時点）**。**ズレを直す想定はこの節（門限・表・上の箇条書きの具体レベル）のドキュメント更新**であり、`range` / `damage` など**弓とクロスで役割を分ける素体設計**（射程の逆転含む）を JSON から方針文へ引きずり戻す意味ではない。門限を変えたら表と本文を JSON に合わせる。

**並べ方**: 表の行順は**弓系ランク低 → 高**（`primaryWeaponMode` と同じ並びの想定）→ クロスボウ。スリングは [`CREATION_POLICY.md`](CREATION_POLICY.md)。

- **`light_crossbow`**: 門限**なし**。**低スキルでも装備**でき、序盤親和を維持する。
- **`crossbow` / `heavy_crossbow`**: Marksman を**段階付け**し、標準とヘビィで門限を分ける（ドラフトでは標準 **5**・ヘビィ **10**）。
- **`long_bow`**: **上級弓**として門限を**最も高く**置く（ドラフト **15**。バニラ長弓 10 より高い想定）。**研究ツリーはバニラのまま**（`Research.json` は本 Mod で触らない方針のとき、その制約下で門限だけ上げる意味を保つ）。
- **Q3・同一 Marksman での総合強さ**: 計画書の連鎖を基準にするが、**横比較は両方装備可能な Marksman 水準**（ドラフトでは原則 **15** 以上）を主に用いる。Marksman が低い帯では `long_bow` が装備できないため、序盤帯は `light_crossbow` / `short_bow` 等の**別検証**とする。
- **上級弓がクロスを「上回る」体感**: **条件を変えた局面**（より高い Marksman・**Q4 以上の品質**・遠距離帯など）で設計し、総合力連鎖の `<=` / `=` と矛盾させない。
- **`light_crossbow` の序盤検証**: 同等・低スキル条件では **`short_bow` の集団より `light_crossbow` の集団の方が強い**ことを目標に含める（集団戦・プレイで確認）。

| `id`             | 本 Mod 予定（ドラフト）                        |
| ---------------- | ---------------------------------------------- |
| `short_bow`      | **なし**（既定）。                             |
| `war_bow`        | `"Marksman"`, **5**（ドラフト・後から変更可）  |
| `curved_bow`     | `"Marksman"`, **10**（ドラフト・後から変更可） |
| `long_bow`       | `"Marksman"`, **15**（ドラフト・後から変更可） |
| `light_crossbow` | **なし**（バニラ同様・低スキル帯）             |
| `crossbow`       | `"Marksman"`, **5**                            |
| `heavy_crossbow` | `"Marksman"`, **10**                           |

---

## 変更するとき

1. **Q3 四種鎖**（`crossbow` / `heavy_crossbow` / `curved_bow` / `long_bow`）と**Equipment**の両方を意識し、矛盾が出ないよう往復する。
2. 数値を変えたら、`tools/plot_weapon_quality_comparison.py` などで品質込みも確認する。
3. 移動ペナルティ・掩体（上記「弓とクロスボウの差別化」の共通箇所）は鎖とは独立に触ってよいが、**素体の火力をヘビィだけ突出**させて鎖や役割説明と矛盾させないこと。
4. **同ティア近接との比較・被弾メタ**を検証に含め、**高品質弓一択**にならないよう帯域・曲線・WQS を確認する。
5. **`requiredSkills`（Marksman）**を変えたら、上記「装備門限」と **Q3 総合強さの検証水準**と整合するか確認する。
